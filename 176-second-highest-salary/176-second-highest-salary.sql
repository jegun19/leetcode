# Write your MySQL query statement below
SELECT IF(COUNT(*) > 1, MIN(SUBQ.SALARY), null) AS SECONDHIGHESTSALARY FROM (SELECT EMPLOYEE.SALARY AS SALARY FROM EMPLOYEE GROUP BY EMPLOYEE.SALARY ORDER BY EMPLOYEE.SALARY DESC LIMIT 2) AS SUBQ